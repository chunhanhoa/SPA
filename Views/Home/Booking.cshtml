@{
    ViewData["Title"] = "Đặt lịch";
    var serviceId = Context.Request.Query["serviceId"].ToString();
}

<div class="container py-5">
    <div class="row">
        <div class="col-lg-6 mb-4">
            <h2 class="mb-4">Đặt Lịch Dịch Vụ</h2>
            <p class="lead">Đặt lịch trước để có trải nghiệm tốt nhất tại LoanSpa</p>
            <img src="~/images/booking-banner.jpg" alt="Booking" class="img-fluid rounded shadow-sm mb-4" style="height: 300px; object-fit: cover;">
            
            <div class="card border-0 bg-light p-4">
                <div class="card-body p-0">
                    <h5 class="card-title">Thông tin liên hệ</h5>
                    <p><i class="fas fa-phone mr-2"></i> 0987 654 321</p>
                    <p><i class="fas fa-envelope mr-2"></i> info@loanspa.com</p>
                    <p><i class="fas fa-map-marker-alt mr-2"></i> 123 Đường Nguyễn Huệ, Q.1, TP.HCM</p>
                </div>
            </div>
        </div>
        
        <div class="col-lg-6">
            <div class="card border-0 shadow-sm">
                <div class="card-body p-4">
                    <h3 class="card-title mb-4">Thông Tin Đặt Lịch</h3>
                    <form id="bookingForm">
                        <div class="form-group mb-3">
                            <label for="fullName">Họ và tên <span class="text-danger">*</span></label>
                            <input type="text" class="form-control" id="fullName" name="fullName" placeholder="Nhập họ và tên" required>
                        </div>

                        <div class="form-group mb-3">
                            <label for="phoneNumber">Số điện thoại <span class="text-danger">*</span></label>
                            <input type="tel" class="form-control" id="phoneNumber" name="phoneNumber" placeholder="Nhập số điện thoại" required>
                        </div>

                        <div class="form-group mb-3">
                            <label for="email">Email <span class="text-danger">*</span></label>
                            <input type="email" class="form-control" id="email" name="email" placeholder="Nhập địa chỉ email" required>
                        </div>
                        
                        <div class="form-group mb-3">
                            <label for="serviceId">Dịch vụ <span class="text-danger">*</span></label>
                            <select class="form-control" id="serviceId" name="serviceId" required>
                                <option value="" selected disabled>Chọn dịch vụ</option>
                                <!-- Các options sẽ được nạp từ API -->
                            </select>
                        </div>
                        
                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-group mb-3">
                                    <label for="bookingDate">Ngày <span class="text-danger">*</span></label>
                                    <input type="date" class="form-control" id="bookingDate" name="bookingDate" required min="@DateTime.Now.ToString("yyyy-MM-dd")">
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-group mb-3">
                                    <label for="bookingTime">Giờ <span class="text-danger">*</span></label>
                                    <select class="form-control" id="bookingTime" name="bookingTime" required>
                                        <option value="" selected disabled>Chọn giờ</option>
                                        <option value="09:00">09:00</option>
                                        <option value="10:00">10:00</option>
                                        <option value="11:00">11:00</option>
                                        <option value="13:30">13:30</option>
                                        <option value="14:30">14:30</option>
                                        <option value="15:30">15:30</option>
                                        <option value="16:30">16:30</option>
                                        <option value="17:30">17:30</option>
                                    </select>
                                </div>
                            </div>
                        </div>
                        
                        <div class="form-group mb-3">
                            <label for="notes">Ghi chú</label>
                            <textarea class="form-control" id="notes" name="notes" rows="3" placeholder="Nhập yêu cầu đặc biệt hoặc ghi chú (nếu có)"></textarea>
                        </div>
                        
                        <button type="submit" class="btn btn-primary btn-lg btn-block mt-4">Đặt Lịch</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            // Lấy danh sách dịch vụ từ API
            $.ajax({
                url: '/api/Services',
                type: 'GET',
                success: function(services) {
                    if (services && services.length > 0) {
                        var options = '';
                        services.forEach(function(service) {
                            var selected = (service.serviceId == "@serviceId") ? 'selected' : '';
                            options += '<option value="' + service.serviceId + '" ' + selected + '>' + 
                                       service.serviceName + ' - ' + new Intl.NumberFormat('vi-VN').format(service.price) + ' VNĐ</option>';
                        });
                        $('#serviceId').append(options);
                    }
                },
                error: function(err) {
                    console.error('Không thể lấy danh sách dịch vụ:', err);
                }
            });

            // Xử lý đặt lịch
            $('#bookingForm').submit(function(e) {
                e.preventDefault();
                
                // Lấy dữ liệu từ form
                var formData = {
                    fullName: $('#fullName').val(),
                    phoneNumber: $('#phoneNumber').val(),
                    email: $('#email').val(),
                    serviceId: $('#serviceId').val(),
                    bookingDate: $('#bookingDate').val(),
                    bookingTime: $('#bookingTime').val(),
                    notes: $('#notes').val()
                };
                
                // Gửi yêu cầu đặt lịch qua API
                $.ajax({
                    url: '/api/Bookings',
                    type: 'POST',
                    contentType: 'application/json',
                    data: JSON.stringify({
                        customerId: 1, // Tạm thời dùng ID 1, sau sẽ lấy từ thông tin đăng nhập
                        serviceIds: { [formData.serviceId]: 1 }, // Key: serviceId, Value: số lượng
                        startTime: formData.bookingDate + 'T' + formData.bookingTime,
                        duration: 60, // Mặc định 60 phút
                        totalAmount: 0 // Tạm thời để 0, sẽ tính ở server
                    }),
                    success: function(response) {
                        alert('Đặt lịch thành công! Chúng tôi sẽ liên hệ với bạn để xác nhận trong thời gian sớm nhất.');
                        $('#bookingForm')[0].reset();
                    },
                    error: function(err) {
                        alert('Có lỗi xảy ra khi đặt lịch. Vui lòng thử lại sau.');
                        console.error('Lỗi đặt lịch:', err);
                    }
                });
            });
        });
    </script>
}
